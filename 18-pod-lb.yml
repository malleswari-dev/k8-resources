apiVersion: v1
kind: Pod
metadata:
  name: service-lb-demo
  labels:
    project: roboshop
    component: frontend
    environment: dev
    purpose: loadbalancer 
 # this labels are used to register this pod with services.   
spec:
  containers:
  - name: nginx
    image: nginx 